export const CormorantClassName = "--font-cormorant";
export const ManropeClassName   = "--font-manrope";
export const SpaceMonoClassName = "--font-space-mono";

const fonts = {
    heading: `var(${CormorantClassName})`,
    body:    `var(${ManropeClassName})`,
    mono:    `var(${SpaceMonoClassName})`,
};

export default fonts;
